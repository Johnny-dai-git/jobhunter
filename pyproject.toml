[project]
name = "job-agent"
version = "0.1.0"
description = "Personal job-search assistant powered by Claude"
requires-python = ">=3.10"
readme = "README.md"

[project.scripts]
job-agent = "src.main:cli"

[tool.setuptools]
packages = ["src", "src.collectors"]
